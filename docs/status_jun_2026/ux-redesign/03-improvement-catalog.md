# 03 · Catálogo de mejoras UX/UI (6 lentes)

_73 propuestas concretas._


## Motion & microinteractions

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Add a global reduced-motion guard + reusable motion keyframes before anything moves | Global (every animated surface) | High | S | This is the foundation gate for every other proposal. globals.css today has ZERO custom @keyframes and the repo has NO prefers-reduced-motion handling anywhere (grep found 0 hits for motion-reduce/reduced-motion). Add an `@layer utilities`  |
| Stagger-in the dashboard pillar cards and needs-attention rows on load | Dashboard | High | S | Right now DashboardContent renders the 4 PillarCards (the `section` grid at line 149) and the NeedsAttention `ul` (line 396) with no entrance motion — content just snaps in after the skeleton. Apply the new `animate-rise` utility to each `< |
| Give pillar cards a hover lift + per-pillar tinted depth instead of a flat ring | Dashboard | High | M | PillarCard (line 245) is a flat `Card` (ring-1 ring-foreground/10, no shadow, no hover feedback) wrapping a fully-static surface even though the whole card deep-links into a pillar. Add `transition-[box-shadow,transform,border-color] durati |
| Add a cross-route content fade so navigating between pillars feels continuous | Global (every route change) | High | S | There is NO app/(app)/template.tsx today (confirmed: find returned none), so route changes hard-cut — the biggest single 'no dynamism' tell when clicking through the sidebar. Add a Next.js App-Router `template.tsx` (re-mounts per navigation |
| Throb the needs-attention status dot to draw the eye without alarming | Dashboard | Medium | S | AttentionRow (line 413) renders a static colored dot (`meta.dot`, the `bg-amber-500`/`bg-rose-500` span at line 426) on operationally-urgent rows — it's the perfect anchor for one restrained signature pulse. Add `animate-throb` (the calm 0. |
| Fade+rise each recent-activity row in as the timeline and 'Load more' pages resolve | Dashboard (Recent Activity) | Medium | M | ActivityRow (line 137) renders timeline `<li>`s with no entrance motion; on initial load and especially on 'Load more' (fetchNextPage, line 102) newly-appended rows pop in abruptly. Apply `animate-rise` to each `<li>` (line 149) with a capp |
| Animate the active nav indicator with a sliding/growing accent rail | Global shell (sidebar) | Medium | M | The active nav item (line 110) only swaps background (`bg-sidebar-accent` + font-medium) via `transition-colors` — there's no motion cue when you navigate, so section changes feel inert. Add a left accent bar: a `before:` pseudo-element on  |
| Upgrade the skeleton from a flat pulse to a directional shimmer sweep | Global (all loading states) | Medium | M | Every list/dashboard/detail loading state uses this one Skeleton, which is just `animate-pulse bg-muted` (flat opacity throb) — functional but the dullest possible loading affordance. Replace with the `animate-shimmer` utility (a moving lin |
| Count-up dashboard headline metrics from 0 to value on first paint | Dashboard | Medium | M | PillarCard's headline number (line 272, `{metric}` in text-2xl tabular-nums) and the AttentionRow count (line 434) render their final value instantly. A short count-up (0 -> N over ~600ms, ease-out) on first mount makes the numbers feel ear |
| Make the destructive Delete button signal a 'heavy, irreversible' operation while pending | Delete / user-offboard dialog (global) | Medium | S | This is the CEO's #1 hotspot and today the pending state is the weakest possible signal: a raw `<ArrowPathIcon className="animate-spin"/>` next to a static 'Delete' label (line 93). For a soft-delete that can cascade (user offboard revokes  |
| Reveal per-row actions on hover/focus and add a press-settle on table rows | Lists (Assets, Users, Consumables, Locations, KB, Applications) | Medium | M | Rows in every ResourceTable are static beyond the table primitive's `hover:bg-muted/50`; the RowActions trigger (line 430, the `EllipsisVerticalIcon` ghost button) is always at full opacity, making dense lists noisy, and there's no affordan |
| Polish the toast: branded enter/exit slide + a richer success beat on destructive flows | Global (Sonner toasts) | Low | S | Sonner is already wired with token colors and heroicons icons (good), but uses default motion and a uniform dwell — the success toast after a delete/offboard (the CEO hotspot) reads as an afterthought. Tune `toastOptions`/Sonner props: a sl |

## Color expression & categorical identity

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Add a token-backed pillar() color helper as the single source of per-pillar identity | Foundation (consumed by sidebar, list headers, dashboard cards, activity feed) | High | M | Create lib/pillar.ts exporting a Pillar union (inventory|access|knowledge|manage) and PILLAR_COLOR mapping each to one chart token via full, non-interpolated class strings (so the Tailwind v4 scanner keeps them): inventory -> --chart-2 (tea |
| Recolor the dashboard pillar cards with per-pillar identity (icon chip + accent) | Dashboard | High | S | In PillarCard (apps/web/app/(app)/dashboard/page.tsx lines 245-313) the icon chip is hardcoded 'bg-primary/10 text-primary' for ALL four cards, so the five-hue identity system is invisible on the product's landing screen. Add a `pillar` pro |
| Replace the activity-feed hardcoded sky/violet/amber chips with chart tokens | Dashboard - Recent activity | High | S | ENTITY_TONE (apps/web/app/(app)/dashboard/_components/recent-activity-panel.tsx lines 130-134) hardcodes 'bg-sky-500/10 text-sky-600 dark:text-sky-400' etc, bypassing the token system AND the chosen pillar palette (sky/violet aren't lazyit  |
| Tint sidebar nav section headings with their pillar hue | App shell - sidebar nav | Medium | M | Section headings in sidebar-nav.tsx (lines 96-101) are uniform 'text-muted-foreground/70 uppercase', so the nav gives zero color wayfinding. Map each NavSection to a pillar via the pillar() helper and add a subtle left-border tint on the se |
| Move asset-history event tones onto tokens (8 hardcoded palette strings) | Asset detail - history timeline | Medium | S | EVENT_TONE (apps/web/app/(app)/assets/_components/asset-history-timeline.tsx lines 30-40) hardcodes emerald/sky/violet/amber/teal/indigo with hand-written dark: variants for 9 event types - the densest single offender. Remap to the semantic |
| Token-ize the dashboard 'Needs attention' tone map (dot + ring) | Dashboard - Needs attention | Medium | S | The TONE record (apps/web/app/(app)/dashboard/page.tsx lines 408-411) hardcodes 'bg-amber-500'/'ring-amber-500/20' and 'bg-rose-500'/'ring-rose-500/30' for the attention dot + ring - the exact 'status hardcoded on the dashboard' the brief c |
| Finish avatar-color coverage so one person = one color on every screen | Asset owners, access grantees, assignee lists, settings user lists | Medium | M | avatarColorFor() (lib/avatar-color.ts) drives the canonical 5-hue identity palette but per the mapping is wired in only ~2-3 places (user-avatar, activity feed). Audit every place a person is rendered - asset detail owner chip, application  |
| Add a pillar-tinted icon chip to each list-page header via PageHeader | List pages (Assets, Consumables, Applications, KB, Users, Locations) | Medium | M | PageHeader (components/page-header.tsx) renders title/subtitle/actions but no leading icon, so list pages (e.g. assets/page.tsx line 314) open with a flat title and no area color. Add an optional `icon` + `pillar` slot to PageHeader that re |
| Add a whisper-soft pillar gradient/tint on card surfaces to kill the flat fill | Dashboard pillar cards + detail panels | Medium | M | Cards are bg-card flat with no depth or hue (Card primitive, ring-1 ring-foreground/10). Without touching the vendored primitive, add a composable wrapper class on the dashboard PillarCard and DetailPanel headers: a very low-opacity linear  |
| Token-ize the BYOI / info banner (raw blue-50/300/600/900) | Users - BYOI account-linking banner | Low | S | byoi-banner.tsx (lines 20-26) is built entirely from raw 'border-blue-300 bg-blue-50 text-blue-900 dark:bg-blue-500/10 ...' - it should consume the --info token, which exists precisely for this informational tone and is AA-paired. Replace w |
| Token-ize the consumable stock-level color (ok/low emerald/amber) | Consumable detail | Low | S | The stock indicator color map at apps/web/app/(app)/consumables/[id]/page.tsx lines 52-53 hardcodes 'ok: text-emerald-600 dark:text-emerald-400' / 'low: text-amber-600 dark:text-amber-400'. These are status semantics, not decoration: remap  |
| Introduce a tinted 'subtle' StatusBadge/Badge variant for low-criticality category labels | Primitive - consumed by lists & detail panels | Low | M | StatusBadge today only fills SOLID (status-badge.tsx lines 24-42) because tinted-on-bone can't clear AA for status text - correct for status. But category/criticality labels in lists are currently either solid status pills (too loud) or pla |

## Practicality & flows

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Turn the ⌘K palette from search-only into a real command palette (actions + quick-nav) | Global command palette (topbar, app-wide) | High | M | components/global-search.tsx today is search-ONLY: it queries GET /search and navigates to records, but exposes zero command items. Add an Actions group at the top of the CommandList that renders BEFORE any query is typed (currently the emp |
| Add a global keyboard shortcut layer (g-then-key nav + new + focus-search) beyond ⌘K | App shell (app)/layout.tsx + a small useGlobalHotkeys hook | High | M | A repo-wide grep confirms ⌘K (in global-search.tsx) is the ONLY keyboard handler in the whole app — no document-level shortcuts exist. Add a tiny client hook mounted once in the shell that listens for: '/' to open the palette focused on sea |
| Saved / pinned views for lists (named filter presets persisted locally) | All resource lists (assets, consumables, applications, kb, users, locations) | High | M | useListParams (lib/hooks/use-list-params.ts) already makes every filter+sort URL-shareable, but there is NO way to save and recall a named view (confirmed: localStorage is used only in settings/roles, never in lists). Add a useSavedViews(re |
| Inline quick-create from empty + filtered-empty states (and dashboard pillar cards) | ResourceTable EmptyState / FilteredEmpty + dashboard PillarCards | Medium | M | EmptyState (components/resource-table.tsx) already takes an `action` CTA, but the filtered-empty branch only offers ClearFiltersLink and several lists pass no action at all. Two changes: (1) when a filtered search yields nothing, add a cont |
| Make whole table rows clickable + reveal row actions on hover/focus | Assets/Consumables/Applications/Users/KB list tables (ResourceTable rows) | Medium | M | In assets/page.tsx (the template every list copies) only the name cell is a <Link>; the other ~70% of the row is dead space, so reaching a record means hitting a narrow target. Add an onClick on TableRow that router.push-es the detail href  |
| Draft autosave / restore for the create forms (asset, article, consumable, application) | asset-form.tsx, kb article-form.tsx, consumable-form.tsx, application-form.tsx (create mode) | Medium | M | Confirmed none of the forms persist input: asset-form.tsx seeds defaultValues only from the record/clone source, and a stray navigation or accidental ⌘K mid-entry loses everything typed. Add a small useFormDraft(key) helper that, in CREATE  |
| Pagination ergonomics: page-size selector + jump-to-page | Pagination footer (components/resource-table.tsx), used by every list | Medium | S | Pagination is prev/next ONLY; the contract supports limit up to 200 (ADR-0030, default 50) and useListParams already exposes setPage + a derived page, but neither is surfaced. Add a compact page-size Select (25 / 50 / 100 / 200) wired to a  |
| Persist & invert the batch bar: keyboard-clear, select-across-pages, and a sticky count | BatchActionBar + useRowSelection on the asset/application lists | Medium | L | BatchActionBar (resource-table.tsx) is sticky and good, but selection is page-local (useRowSelection over visibleIds) and there's no keyboard affordance: pressing Escape should clear the selection, and when allSelected on a page that's part |
| Inline status edit on asset rows (and access-level on grant rows) without opening the form | assets/page.tsx status cell; applications/[id] grant rows | Medium | M | Changing one asset's status today means RowActions -> Edit -> full /assets/[id]/edit route -> save. The batch bar already proves a Select can drive useBatchSetAssetStatus inline; expose the SAME affordance per-row by making the AssetStatusB |
| Two-phase offboard with an impact preview instead of a single soft-delete confirm | users/[id]/page.tsx DeleteConfirmDialog (offboard) | Medium | M | The offboard reuses the generic DeleteConfirmDialog with a sentence of warning copy ('revokes their access grants and releases their assets') but shows NO counts, even though the user detail page already loads the user's active assignments  |
| Persistent recent-items / quick-jump rail in the palette and sidebar | global-search.tsx palette + sidebar-nav.tsx | Low | M | There's no notion of 'where was I' — every visit to a record goes through search or a list. Track the last ~6 visited detail routes (asset/app/article/user) in localStorage on each detail-page mount, and render them as a 'Recent' CommandGro |
| Copy-to-clipboard affordances on identifiers (asset tag, serial, IDs, emails) | Asset/User/Application detail panels (detail-panel.tsx DetailField) + asset list mono cells | Low | S | Asset tags and serials render as plain `font-mono` text (assets/page.tsx) and detail fields are read-only; copying an asset tag or a user email into a ticket/chat means manual select-drag. Add a tiny CopyButton (heroicons ClipboardIcon 16/s |

## States & delight

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Give EmptyState character: pillar-tinted illustration ring, scale-in entrance, richer layout | All resource lists (Assets, Consumables, Applications, KB, Users, Locations) + settings managers | High | M | Evolve the single shared EmptyState in components/resource-table.tsx (lines 541-567), so every list inherits it. Today it is a flat size-12 bg-muted circle + size-6 icon + two text lines. Upgrade to a fuller 'moment': enlarge to a size-16/2 |
| Stop the dead-zone flash: add a celebratory, token-correct setup-complete moment | First-run setup wizard — Done step (/setup) | High | S | step-done.tsx is the single highest-delight moment in the product and it (a) hardcodes text-emerald-600 dark:text-emerald-400 (token-discipline violation — should be --success) and (b) is a static checkmark. Replace the raw color with text- |
| Make destructive confirmation feel weightier and offboard impact legible | Delete/offboard confirmation (global DeleteConfirmDialog + Users detail offboard) | High | M | Two changes in delete-confirm-dialog.tsx. (1) Pending state: the generic ArrowPathIcon spinner reads as a normal load — for a destructive op, add a brief shimmer/pulse on the 'Delete' label while isPending (a subtle opacity 1->0.7->1 loop o |
| Author a single prefers-reduced-motion + delight-keyframes layer so new motion is safe by default | Foundation (globals.css) — underpins every proposal above | High | S | There is currently zero authored @keyframes / prefers-reduced-motion handling in globals.css (all motion is borrowed from Radix primitives). Before adding signature moments, add a small @layer utilities block defining the reusable keyframes |
| Upgrade success toasts to a brief celebration for create/offboard/restore | App-wide (Sonner toaster) — create, delete/restore, batch, grant flows | Medium | S | components/ui/sonner.tsx already maps a CheckCircleIcon success icon but the success toast is visually identical to info. Add a `cn-toast` success treatment in globals.css: a 1px left accent bar in --success, and a one-shot icon 'pop' (scal |
| Give the 404 a friendly, on-brand personality instead of a bare mono code | Global not-found (/app/not-found.tsx) | Medium | S | Today it's a mono '404', a heading, a line and a button — deliberately spartan. Add tasteful character: an oversized, low-chroma glyph treatment (e.g. a large MagnifyingGlassIcon or a stylized '404' set in a soft pillar-tinted medallion usi |
| Make ErrorState + app error boundary feel handled, not broken | ErrorState (resource-table.tsx) + authenticated error boundary ((app)/error.tsx) | Medium | S | Both are static dashed boxes. Add: (1) a soft entrance (fade+rise, ~200ms) so the surface arrives intentionally rather than snapping in; (2) on the Retry/Try-again button, spin the ArrowPathIcon for the duration of the retry click (brief `a |
| Quality loading: staggered skeleton shimmer instead of a uniform pulse | All list loads (ResourceTable skeletons), dashboard skeleton, detail panels | Medium | M | The Skeleton primitive (components/ui/skeleton.tsx) is a single animate-pulse; every row/card pulses in perfect unison, which reads cheap. Add an opt-in shimmer/stagger: define a `.skeleton-shimmer` keyframe (a translating highlight gradien |
| Helpful zero-results in global search: suggestions + scoped-search recovery | Global command palette (components/global-search.tsx) | Medium | S | The no-results branch (StatusRow 'No results for X', line 225) is a dead end. Make it helpful: render a small EmptyState-style block with a MagnifyingGlassIcon medallion, the query echoed, and recovery affordances — if the search is scoped  |
| Animate the 'Needs attention' beat so urgency is felt, calmly | Dashboard — Needs attention zone (dashboard/page.tsx) | Medium | M | AttentionRow (lines 413-438) already has a tone dot, but it (a) hardcodes raw Tailwind (bg-amber-500/bg-rose-500, ring-amber-500/20) instead of --warning/--destructive tokens, and (b) is fully static. Two tasteful adds: map TONE to the sema |
| Distinguish the filtered-empty state as a recoverable moment, not 'no data' | ResourceTable filtered-empty rows (assets/consumables/applications/users/locations/kb) | Medium | S | FilteredEmpty (resource-table.tsx:273-286) is a centered line + optional Clear action inside a 24px-tall table cell and a dashed mobile box — visually it's nearly identical to a real EmptyState, which confuses 'you filtered everything out'  |
| Confident, animated empty-success on Settings managers and service accounts | Settings — Categories / Asset models / Service accounts managers | Low | S | category-manager.tsx (line 149), asset-model-manager.tsx (line 120) and service-accounts-manager.tsx (line 176) all use the bare EmptyState. Beyond inheriting the upgraded EmptyState (proposal 1), give these admin-setup screens a first-run  |

## Data-viz & dashboards

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Asset-status donut on the Assets pillar card (CSS conic-gradient, no lib) | Dashboard / Assets pillar card | High | M | The Assets PillarCard already receives assets.byStatus (zero-filled across all 6 AssetStatus values from DashboardSummary). Replace the bare headline number with a small (size-16/20) ring donut rendered via a single CSS conic-gradient built |
| Stock-level gauge bar on consumable detail (current vs reorder threshold) | Consumables / detail — Stock panel | High | S | The Stock DetailPanel shows a 4xl number + a colored status word. Add a horizontal gauge bar (a div track + filled span, width = currentStock / max(currentStock, minStock*2 or a sensible cap) as %) with a vertical tick marker at the minStoc |
| Estate-wide KPI strip above the pillar cards (4 stat tiles with proportion bars) | Dashboard / above pillar grid | High | M | Add a compact 4-up KPI row between QuickActions and NeedsAttention: 'Operational %' (assets.byStatus.OPERATIONAL / assets.total), 'Assigned %' (assets.assigned / assets.total), 'Published %' (articles.published / articles.total), 'In stock  |
| Stock-over-time sparkline from the movement ledger (client-derived, SVG) | Consumables / detail — Movements panel | Medium | M | useConsumableMovements(id) already returns the full ledger (IN/OUT/ADJUSTMENT, newest-first). Derive a running-stock series client-side by walking movements oldest→newest (ADJUSTMENT sets absolute, IN adds, OUT subtracts) and render a ~120x |
| Per-pillar mini bar/segmented breakdowns on the Knowledge & Access cards | Dashboard / Knowledge + Access pillar cards | Medium | M | Knowledge card: render a single 2-segment stacked bar (published vs draft from articles.published/articles.draft) under the headline — published = var(--chart-3) green, draft = var(--muted). Access card: render a 3-segment proportional bar  |
| Activity volume sparkbars on the Recent Activity panel header (client-aggregated) | Dashboard / Recent activity panel | Medium | M | The panel already flattens fetched activity pages into `items` (each with occurredAt + entityType). Aggregate the loaded items into per-day buckets for the last ~14 days and render a small CSS column micro-bar strip in the CardHeader (right |
| Recolor the activity-feed pillar icons to the categorical tokens (viz consistency fix) | Dashboard / Recent activity rows | Medium | S | ENTITY_TONE currently hardcodes raw Tailwind sky/violet/amber with hand-written dark: variants — the exact token-discipline break the mandate flags. Remap asset→var(--chart-2) teal, application→var(--chart-1) indigo, consumable→var(--chart- |
| Access-expiry horizon strip on the Applications list (buckets: ≤7d / ≤30d / ≤90d / later) | Access / Applications list header | Medium | L | Grants carry expiresAt (nullable, informative-only per ADR-0023). Add a thin segmented 'expiry horizon' bar to the Applications list header: bucket active grants by expiresAt into ≤7d (var(--destructive)), ≤30d (var(--warning)), ≤90d (var(- |
| Throb/pulse the Needs-attention dot proportional to severity (motion as signal) | Dashboard / Needs attention rows | Medium | S | AttentionRow's TONE map already hardcodes bg-amber-500 / bg-rose-500 dots — migrate to var(--warning)/var(--destructive) tokens (discipline fix) and add a gentle tw-animate-css pulse ONLY on danger-tone dots (lost assets), so the eye is dra |
| Backend: activity & expiry stats endpoints to power faithful heat/horizon viz (NEW CONTRACT — flag) | Dashboard + Applications (data layer) | Medium | L | Two of the viz above (all-time activity heat, full access-expiry horizon) are only approximate from client-fetched pages. Propose two read-only, derived endpoints (parallel to the existing /dashboard/summary aggregation, no schema change):  |
| Sparkline cells in the consumables list (stock trend per row) | Consumables / list (ResourceTable) | Low | L | Add an optional 'Trend' column rendering the same SVG running-stock sparkline as the detail page, per consumable. This needs per-row movement history which the list endpoint does NOT return — so it requires EITHER a backend addition (the pr |
| KB growth sparkline (articles created/published over time) | Knowledge / KB list header (and/or Knowledge pillar card) | Low | M | Articles carry createdAt + publishedAt. A 'KB growth' cumulative-count sparkline (articles over the last N weeks) would visualize knowledge accumulation. Client aggregation is feasible only over the currently-fetched page; a faithful cumula |
| Library recommendation note: stay no-lib now; reach for a 5kb sparkline lib only if viz proliferates | Cross-cutting (dependency decision) | Low | S | RECOMMENDATION (decision, not code): every viz above is achievable with pure CSS (conic-gradient donut, flexbox proportion bars) + hand-rolled inline SVG polylines, reading the existing --chart-1..5 / --success / --warning / --info / --dest |

## Hierarchy, typography, depth & layout

| Propuesta | Pantalla | Impacto | Esfuerzo | Cambio |
| --- | --- | --- | --- | --- |
| Introduce an elevation scale (--surface-1/2/3 shadow tokens) and apply it to Card/DetailPanel/sticky bars | All surfaces — cards, detail panels, batch bar, dropdowns | High | M | In apps/web/app/globals.css define a warm-tinted shadow scale as tokens (e.g. --shadow-sm: 0 1px 2px oklch(0.21 0.006 75 / 0.06); --shadow-md: 0 2px 8px oklch(0.21 0.006 75 / 0.08); --shadow-lg: 0 8px 24px oklch(0.21 0.006 75 / 0.12)) with  |
| Give cards a hover-lift so the dashboard pillar cards and list cards feel interactive | Dashboard pillar cards, mobile ResourceCard | High | S | The four PillarCards (dashboard/page.tsx line 265, `<Card className="h-full">`) and AttentionRows are clickable surfaces but read as static paper — no elevation change on hover. Add a `transition-[box-shadow,transform] duration-150 hover:-t |
| Establish a named type scale for hero metrics vs labels to sharpen number/label hierarchy | Dashboard pillar cards & Needs-attention counts; reports KPIs (future) | High | M | Big numbers and labels are pixel-guessed inline today: the pillar metric is `text-2xl font-semibold tabular-nums` jammed into a CardDescription (dashboard/page.tsx line 272), while AttentionRow counts are only `text-base font-semibold` (lin |
| Add an eyebrow/kicker + optional icon slot to PageHeader so list pages get presence and pillar identity | Every list & detail page header (Assets, KB, Users, Consumables, Applications, Dashboard) | High | M | page-header.tsx renders a single `text-2xl font-semibold` h1 with a muted subtitle (lines 50-55) — correct and consistent, but flat: no eyebrow, no leading icon, no accent. Extend PageHeader with an optional `eyebrow` (small uppercase track |
| Increase table row rhythm and add zebra/hover depth for scannable lists | Assets/Users/Consumables/Applications/Locations/KB tables | Medium | M | TableCell is `p-2` and rows are `h-10` headers with only `hover:bg-muted/50` (ui/table.tsx lines 60, 81-86) — dense and low-contrast, so long asset lists (assets/page.tsx) read as an undifferentiated grid. Since the primitive can't be hand- |
| Stratify the page background vs content surface so cards read as elevated | App shell main content area; all pages | Medium | S | The shell `<main>` and the Card both sit at near-identical lightness (--background bone 0.985 vs --card 0.995 light; 0.205 vs 0.255 dark) with no shadow, so on light mode cards barely separate from the canvas. Pair the new elevation tokens  |
| Promote DetailPanel section headings and give panels a header divider for hierarchy | Detail pages: assets/[id], users/[id], applications/[id], consumables/[id], kb/[slug] | Medium | S | DetailPanel's title is `text-sm font-semibold` (detail-panel.tsx line 36) — the same weight/size as some field values, so on a multi-panel page (e.g. users/[id] with Profile / Assets held / Access / Authored panels) the sections don't estab |
| Differentiate the 'Needs attention' zone with elevation and tone so it reads as the priority band | Dashboard — Needs attention section | Medium | M | AttentionRows are plain `rounded-lg border p-3` with only a faint colored ring (dashboard/page.tsx lines 408-422) and hardcoded `bg-amber-500/ring-amber-500` (line 409) — they don't out-rank the pillar cards below them despite being the mos |
| Replace hardcoded activity-feed pillar colors with chart tokens and add date-group headers | Dashboard — Recent activity timeline | Medium | M | The feed hardcodes raw Tailwind hues with hand-written dark: variants (recent-activity-panel.tsx lines 130-134: `bg-sky-500/10 text-sky-600 dark:text-sky-400` etc.), breaking the token system, and renders one long undifferentiated `<ol>` (l |
| Add a sticky page-header treatment + max-content-width to long pages for spatial anchoring | App shell content; long detail pages (assets/[id], users/[id]) | Medium | M | The `<main>` is a full-bleed `p-4 md:p-6` with no max width (layout.tsx line 64), so on wide monitors tables and detail grids stretch edge-to-edge and the PageHeader scrolls away on long pages, losing context. Constrain content to a comfort |
| Upgrade skeletons to a depth-matched, staggered shimmer that mirrors final layout elevation | All loading states (dashboard, tables, detail pages) | Low | M | Skeletons are flat `animate-pulse bg-muted` blocks (DetailSkeleton in detail-panel.tsx, the table skeletons in resource-table.tsx lines 176-188, dashboard skeleton at page.tsx 444-472) that don't carry the same ring/shadow as the real surfa |
| Tighten vertical rhythm and add section spacing scale across list pages | List pages (assets/page.tsx and siblings) | Low | M | List pages stack `space-y-6` at the top with an inner `space-y-6`/`flex gap-3` filter bar (assets/page.tsx lines 312-313, 366), but the filter bar, active-filter chips, table and pagination all sit at the same flat spacing with no grouping, |

